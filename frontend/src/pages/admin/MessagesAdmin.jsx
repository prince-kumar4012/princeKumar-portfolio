import { useEffect,useState } from 'react';import api from '../../services/api';
export default function MessagesAdmin(){const[items,setItems]=useState([]);useEffect(()=>{api.get('/messages').then(({data})=>setItems(data.messages||data)).catch(()=>{})},[]);return <div><h1 className="text-3xl font-black mb-6">Messages</h1><div className="grid gap-4">{items.length===0&&<p className="text-slate-400">No messages found.</p>}{items.map(m=><div className="card p-5" key={m._id}><b>{m.name}</b><p className="text-sky-400">{m.email}</p><p className="text-slate-300 mt-3">{m.message}</p></div>)}</div></div>}

import { useState } from 'react';
import SectionTitle from './SectionTitle';
import api from '../services/api';
export default function Contact(){const[form,setForm]=useState({name:'',email:'',subject:'Portfolio Contact',message:''});const[status,setStatus]=useState('');const submit=async(e)=>{e.preventDefault();setStatus('Sending...');try{await api.post('/messages',form);setStatus('Message sent successfully');setForm({name:'',email:'',subject:'Portfolio Contact',message:''})}catch{setStatus('Backend not connected. Start backend server.')}};return <section id="contact" className="section"><div className="container"><SectionTitle tag="Contact" title="Let’s Work Together"/><form onSubmit={submit} className="card p-6 max-w-3xl mx-auto grid gap-4"><input className="input" placeholder="Your Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><input className="input" type="email" placeholder="Your Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><input className="input" placeholder="Subject" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/><textarea className="input min-h-36" placeholder="Message" value={form.message} onChange={e=>setForm({...form,message:e.target.value})} required/><button className="btn btn-primary justify-center">Send Message</button>{status&&<p className="text-center text-sky-300">{status}</p>}</form></div></section>}
